name: Data Pipeline

on: [push]

jobs: 
  data_refresh: 
    name: Data refresh
    runs-on: ubuntu-latest 
    env:
      SEASON_YEAR: 2019
    steps:
    # check-out the repository <stat-scrape> so job can access all your code
    - uses: actions/checkout@v2
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with: 
        python-version: 3.9.7
      # install poetry 
    - name: Install Poetry
      uses: snok/install-poetry@v1
      with:
        virtualenvs-create: true
        virtualenvs-in-project: true
        installer-parallel: true
    # if an environment already exists, load it; otherwise create a new one 
    - name: Load Cached Virtual Environment
      id: cached-poetry-dependencies
      uses: actions/cache@v2
      with:
        path: .venv
        key: venv-${{ runner.os }}-${{ hashFiles('**/poetry.lock') }}
    - name: Install dependencies
      if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
  # if no cache exists, install packages 
      run: poetry install --no-interaction --no-root
    - name: NFL data refresh
      run: |
        source .venv/bin/activate
        python src/fantasyfootball/pipeline/collect/collect_calendar.py --season_year $SEASON_YEAR
        python src/fantasyfootball/pipeline/process/process_calendar.py --season_year $SEASON_YEAR
        python src/fantasyfootball/pipeline/load/load_data.py --season_year $SEASON_YEAR
    - name: Commit new data to datasets directory
      shell: bash
      run: |
        echo "Loading data to git"
        if [[ "$(git status --porcelain)" != "" ]]; then
            git config --global user.name 'MarkLeBoeuf'
            git config --global user.email 'markleboeuf2015@u.northwestern.edu'
            git add datasets/*.csv        
            git commit -m "Auto update historical NFL data"
            git push
        fi


